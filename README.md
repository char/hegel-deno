# hegel-deno

`hegel-typescript` has to run under Node.js because it uses Koffi for FFI,
but I mostly use Deno. This is an adaptation of the library using Deno's built-in FFI.

Check out [hegel.dev](https://hegel.dev)!
